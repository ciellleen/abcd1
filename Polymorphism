
다형성(polymorphism)이란 "많은 형태"라는 뜻이며, 프로그래밍에서는 동일한 이름을 가진 메서드/함수/연산자가 여러 객체나 클래스에서 실행될 수 있는 특성을 가리킵니다.

함수 다형성

파이썬에서 다른 객체에 사용할 수 있는 함수의 예로는 len() 함수가 있습니다.

문자열: len()은 문자의 개수를 반환합니다.
튜플: len()은 튜플에 포함된 항목의 개수를 반환합니다.
딕셔너리: len()은 딕셔너리의 키/값 쌍의 개수를 반환합니다.
클래스 다형성

클래스 메서드에서 다형성은 자주 사용됩니다. 여러 클래스에 같은 메서드 이름이 있을 수 있습니다.

예를 들어, Car, Boat, Plane 세 개의 클래스가 있고 모두 move() 메서드를 가질 수 있습니다.
상속 클래스 다형성

부모 클래스와 그 자식 클래스들 사이에서도 다형성을 사용할 수 있습니다.

Vehicle이라는 부모 클래스를 만들고 Car, Boat, Plane을 Vehicle의 자식 클래스로 만들 수 있습니다. 자식 클래스는 부모 클래스의 메서드를 상속받지만, 필요에 따라 메서드를 재정의할 수 있습니다.
